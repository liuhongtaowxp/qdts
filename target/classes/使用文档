原理
1.定时扫描t_qd_consumer表,查询出状态为prepare（即没有调用成功的）,并且调用时间和当时时间相差15分钟以上的记录
2.找到method字段，利用发射调用消费方controller的方法，传入参数，携带组ID
3.调用到提供方时，先用组ID查询t_qd_provider表，是否执行成功过，如果成功过直接返回result
  如果不成功，就执行

1.在消费方和提供方分别加入jar
2.在消费方创建t_qd_consumer表
3.在提供方创建t_qd_provider表
4.在消费方controller加入QdTransactionConsumer注解
5.在提供方controller加入QdTransactionProvider注解
6.消费方调用提供方使用jar中的容器提供的RestTemplate
7.消费方和提供方springboot入口加入@ComponentScan("com.istonesoft")
8.消费方和提供方controller返回参数必须为QdResult